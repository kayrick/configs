mpc stop
echo "Mounting private"
[ -e ~/private/MOUNTED ] || encfs ~/.private ~/private
echo "Mounting Dropbox"
[ -e ~/Documents/MOUNTED ] || cat ~/private/DROPBOX | encfs ~/Sync/Documents/ ~/Documents -S
